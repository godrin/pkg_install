# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'pkg_install' do
  developer('FIX', 'FIX@example.com')

  self.rubyforge_name = 'pkg_install' # if different than 'pkg_install'
  self.extra_deps << ['facter','1.5.7']
end

# vim: syntax=ruby
